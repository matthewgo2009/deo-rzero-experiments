$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
type: command
display_name: deo-curriculum-rerun-persist
experiment_name: deo-rzero-7set
description: "Re-run DEO curriculum with cp persistence so checkpoints survive; dual-grade afterwards."
compute: azureml:azureml-job-cluster
environment:
  build:
    path: ./env
    dockerfile_path: Dockerfile
code: ..
command: bash azureml/run_pipeline_job.sh ${{outputs.work}}
environment_variables:
  HF_TOKEN: __HF_TOKEN__
  OPENAI_API_KEY: __OPENAI_API_KEY__
  HUGGINGFACENAME: yuyang322
  MODE: curriculum
  DEO_ABBR: "deo_curriculum"
  DEO_BETA_SCHEDULE: "1.0,0.5,0.25,0.1,0.05"
resources:
  instance_type: Standard_ND96isr_H100_v5
  instance_count: 1
outputs:
  work:
    type: uri_folder
    mode: rw_mount
    path: azureml://datastores/workspaceblobstore/paths/yyd_curriculum_rerun/
